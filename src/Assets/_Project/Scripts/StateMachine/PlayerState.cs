namespace Fenrir.StateMachine
{
    public enum PlayerState
    {
        Idle,
        Moving,
        Dodging,
        Attacking,
        Blocking,
        Staggered,
        Dead,
        Interacting,
        InDialogue,
        InEvolution,
    }
}
